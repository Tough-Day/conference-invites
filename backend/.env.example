# Database
DATABASE_URL="postgresql://user:password@localhost:5432/conference_invites?schema=public"

# Server
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Short URL API (optional - will fallback to full URL if not configured)
SHORT_URL_API_KEY=
SHORT_URL_API_ENDPOINT=

# HubSpot Integration (optional)
HUBSPOT_API_KEY=

# Google Cloud (for production)
GCS_BUCKET_NAME=
GCS_PROJECT_ID=
